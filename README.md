# benchkit
System performance benchmarking toolkit for Linux environments.
Measures memory latency, I/O throughput, and kernel scheduling overhead.

## Usage
```bash
chmod +x benchkit
sudo ./benchkit
```

## Requirements
- Linux x86_64
- Root access (for hardware counter access)
